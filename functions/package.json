{
  "name": "entrestate-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "deploy": "firebase deploy --only functions",
    "emulate": "firebase emulators:start --only functions,firestore"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}