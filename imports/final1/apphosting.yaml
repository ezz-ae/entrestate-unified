run_config:
  runtime: nodejs20

services:
  - name: default
    run_from_source:
      build_command: "npm ci || npm install && npm run build"
      run_command: "npm run start"
      env:
        - key: PORT
          value: "8080"
